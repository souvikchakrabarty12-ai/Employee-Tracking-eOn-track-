{
  "name": "eon-track-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "echo 'Run api and web independently'",
    "lint": "echo 'lint placeholder'",
    "test": "echo 'test placeholder'",
    "db:seed": "node scripts/seed-demo.js"
  }
}
